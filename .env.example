##########################################
# 🌱 PERFIL LOCAL - H2 DATABASE (DEV)
##########################################

# URL de conexão para o banco de dados em memória H2
SPRING_DATASOURCE_URL=jdbc:h2:mem:DB_DIMDIM

# Usuário padrão (pode ser qualquer valor)
SPRING_DATASOURCE_USERNAME=admin

# Senha do H2 (geralmente vazia)
SPRING_DATASOURCE_PASSWORD=

# Driver JDBC do H2
SPRING_DATASOURCE_DRIVER=org.h2.Driver

# Dialeto do Hibernate para H2
SPRING_JPA_DATABASE_PLATFORM=org.hibernate.dialect.H2Dialect

# Estratégia de geração do schema (create / update / validate / none)
SPRING_JPA_HIBERNATE_DDL_AUTO=create


##########################################
# ☁️ PERFIL PRODUÇÃO - AZURE SQL SERVER
##########################################
# ⚠️ Descomente estas linhas ao fazer deploy no Azure
##########################################

# SPRING_DATASOURCE_URL=jdbc:sqlserver://sql-server-dimdim.database.windows.net:1433;databaseName=db-dimdim;encrypt=true;trustServerCertificate=false;loginTimeout=30;
# SPRING_DATASOURCE_USERNAME=user-dimdim
# SPRING_DATASOURCE_PASSWORD=ColoqueSuaSenhaAqui
# SPRING_JPA_DATABASE_PLATFORM=org.hibernate.dialect.SQLServerDialect
# SPRING_JPA_HIBERNATE_DDL_AUTO=update
# SPRING_DATASOURCE_DRIVER=com.microsoft.sqlserver.jdbc.SQLServerDriver


##########################################
# ⚙️ OUTRAS CONFIGURAÇÕES OPCIONAIS
##########################################

# Porta padrão da aplicação (caso queira alterar)
# SERVER_PORT=8080

# Logging detalhado para debug (opcional)
# LOGGING_LEVEL_ORG_SPRINGFRAMEWORK=DEBUG
# LOGGING_LEVEL_COM_FIAP_DEVOPS_DIMDIM=DEBUG
